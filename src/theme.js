import { extendTheme } from '@chakra-ui/react';

export const theme = extendTheme({
  colors: {
    teal: {
      500: '#a4ffaf',
    },
  },
  fonts: {
    body: 'JetBrainsMono Bold, sans-serif',
    heading: 'JetBrainsMono Bold, sans-serif',
  },
});